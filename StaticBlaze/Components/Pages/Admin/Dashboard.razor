@page "/Admin/Dashboard"
@using Blazorise.Animate

<div class="space-y-6 transition-all duration-300 ease-in-out">
    <!-- Welcome Section -->
    <div class="bg-white dark:bg-gray-800 rounded-2xl p-6 shadow-sm hover:shadow-md transition-all duration-300">
        <Animate Animation="Animations.FadeDown" Duration="TimeSpan.FromSeconds(0.5)">
            <h1 class="text-2xl font-bold bg-gradient-to-r from-blue-600 to-purple-600 bg-clip-text text-transparent">Welcome back, Admin!</h1>
            <p class="mt-2 text-gray-600 dark:text-gray-300">Here's what's happening with your blog today.</p>
        </Animate>
    </div>

    <!-- Stats Grid -->
    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-6">
        <!-- Total Posts -->
        <div class="bg-white dark:bg-gray-800 rounded-2xl p-6 shadow-sm hover:shadow-md transition-shadow">
            <div class="flex items-center justify-between">
                <div>
                    <p class="text-sm font-medium text-gray-600 dark:text-gray-400">Total Posts</p>
                    <p class="text-2xl font-bold text-gray-900 dark:text-white">@Stats.TotalPosts</p>
                </div>
                <div class="p-3 bg-blue-50 dark:bg-blue-900/20 rounded-xl">
                    <svg class="w-6 h-6 text-blue-600 dark:text-blue-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 20H5a2 2 0 01-2-2V6a2 2 0 012-2h10a2 2 0 012 2v1m2 13a2 2 0 01-2-2V7m2 13a2 2 0 002-2V9.5a2 2 0 00-2-2h-2"></path>
                    </svg>
                </div>
            </div>
            <div class="mt-4 flex items-center text-sm">
                <span class="@(Stats.PostsGrowth >= 0 ? "text-green-500" : "text-red-500") flex items-center">
                    <svg class="w-4 h-4 mr-1 @(Stats.PostsGrowth < 0 ? "transform rotate-180" : "")" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 7h8m0 0v8m0-8l-8 8-4-4-6 6"></path>
                    </svg>
                    @Math.Abs(Stats.PostsGrowth)%
                </span>
                <span class="ml-2 text-gray-600 dark:text-gray-400">vs last month</span>
            </div>
        </div>

        <!-- Total Views -->
        <div class="bg-white dark:bg-gray-800 rounded-2xl p-6 shadow-sm hover:shadow-md transition-shadow">
            <div class="flex items-center justify-between">
                <div>
                    <p class="text-sm font-medium text-gray-600 dark:text-gray-400">Total Views</p>
                    <p class="text-2xl font-bold text-gray-900 dark:text-white">@Stats.TotalViews</p>
                </div>
                <div class="p-3 bg-purple-50 dark:bg-purple-900/20 rounded-xl">
                    <svg class="w-6 h-6 text-purple-600 dark:text-purple-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 12a3 3 0 11-6 0 3 3 0 016 0z"></path>
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M2.458 12C3.732 7.943 7.523 5 12 5c4.478 0 8.268 2.943 9.542 7-1.274 4.057-5.064 7-9.542 7-4.477 0-8.268-2.943-9.542-7z"></path>
                    </svg>
                </div>
            </div>
            <div class="mt-4 flex items-center text-sm">
                <span class="@(Stats.ViewsGrowth >= 0 ? "text-green-500" : "text-red-500") flex items-center">
                    <svg class="w-4 h-4 mr-1 @(Stats.ViewsGrowth < 0 ? "transform rotate-180" : "")" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 7h8m0 0v8m0-8l-8 8-4-4-6 6"></path>
                    </svg>
                    @Math.Abs(Stats.ViewsGrowth)%
                </span>
                <span class="ml-2 text-gray-600 dark:text-gray-400">vs last month</span>
            </div>
        </div>

        <!-- Active Users -->
        <div class="bg-white dark:bg-gray-800 rounded-2xl p-6 shadow-sm hover:shadow-md transition-shadow">
            <div class="flex items-center justify-between">
                <div>
                    <p class="text-sm font-medium text-gray-600 dark:text-gray-400">Active Users</p>
                    <p class="text-2xl font-bold text-gray-900 dark:text-white">@Stats.ActiveUsers</p>
                </div>
                <div class="p-3 bg-green-50 dark:bg-green-900/20 rounded-xl">
                    <svg class="w-6 h-6 text-green-600 dark:text-green-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 20h5v-2a3 3 0 00-5.356-1.857M17 20H7m10 0v-2c0-.656-.126-1.283-.356-1.857M7 20H2v-2a3 3 0 015.356-1.857M7 20v-2c0-.656.126-1.283.356-1.857m0 0a5.002 5.002 0 019.288 0M15 7a3 3 0 11-6 0 3 3 0 016 0zm6 3a2 2 0 11-4 0 2 2 0 014 0zM7 10a2 2 0 11-4 0 2 2 0 014 0z"></path>
                    </svg>
                </div>
            </div>
            <div class="mt-4 flex items-center text-sm">
                <span class="@(Stats.UsersGrowth >= 0 ? "text-green-500" : "text-red-500") flex items-center">
                    <svg class="w-4 h-4 mr-1 @(Stats.UsersGrowth < 0 ? "transform rotate-180" : "")" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 7h8m0 0v8m0-8l-8 8-4-4-6 6"></path>
                    </svg>
                    @Math.Abs(Stats.UsersGrowth)%
                </span>
                <span class="ml-2 text-gray-600 dark:text-gray-400">vs last month</span>
            </div>
        </div>

        <!-- Comments -->
        <div class="bg-white dark:bg-gray-800 rounded-2xl p-6 shadow-sm hover:shadow-md transition-shadow">
            <div class="flex items-center justify-between">
                <div>
                    <p class="text-sm font-medium text-gray-600 dark:text-gray-400">Comments</p>
                    <p class="text-2xl font-bold text-gray-900 dark:text-white">@Stats.TotalComments</p>
                </div>
                <div class="p-3 bg-yellow-50 dark:bg-yellow-900/20 rounded-xl">
                    <svg class="w-6 h-6 text-yellow-600 dark:text-yellow-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 8h2a2 2 0 012 2v8a2 2 0 01-2 2H7l-4 4V10a2 2 0 012-2h2m10-4h-4a2 2 0 00-2 2v4a2 2 0 002 2h4a2 2 0 002-2V6a2 2 0 00-2-2z"></path>
                    </svg>
                </div>
            </div>
            <div class="mt-4 flex items-center text-sm">
                <span class="@(Stats.CommentsGrowth >= 0 ? "text-green-500" : "text-red-500") flex items-center">
                    <svg class="w-4 h-4 mr-1 @(Stats.CommentsGrowth < 0 ? "transform rotate-180" : "")" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 7h8m0 0v8m0-8l-8 8-4-4-6 6"></path>
                    </svg>
                    @Math.Abs(Stats.CommentsGrowth)%
                </span>
                <span class="ml-2 text-gray-600 dark:text-gray-400">vs last month</span>
            </div>
        </div>
    </div>

    <!-- Content Grid -->
    <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
        <!-- Recent Posts -->
        <div class="bg-white dark:bg-gray-800 rounded-2xl p-6 shadow-sm hover:shadow-md transition-all duration-300">
            <div class="flex items-center justify-between mb-4">
                <h2 class="text-lg font-semibold text-gray-900 dark:text-white">Recent Posts</h2>
                <button @onclick="NavigateToNewPost" class="text-blue-600 dark:text-blue-400 hover:underline text-sm">Create New</button>
            </div>
            <div class="space-y-4">
                @if (RecentActivity == null || !RecentActivity.Any())
                {
                    <div class="text-center py-8">
                        <p class="text-gray-500 dark:text-gray-400">No recent posts found</p>
                        <button @onclick="NavigateToNewPost" class="mt-2 px-4 py-2 bg-blue-600 text-white rounded-lg hover:bg-blue-700 transition-colors">Create Your First Post</button>
                    </div>
                }
                else
                {
                    @foreach (var post in RecentPosts)
                    {
                        <Animate Animation="Animations.FadeIn" Duration="TimeSpan.FromSeconds(0.3)">
                            <div class="flex items-center space-x-4 p-4 rounded-xl hover:bg-gray-50 dark:hover:bg-gray-700/50 transition-colors">
                                <div class="flex-1">
                                    <h3 class="text-sm font-medium text-gray-900 dark:text-white">@post.Title</h3>
                                    <p class="text-sm text-gray-500 dark:text-gray-400">@post.ShortDescription</p>
                                    <div class="flex items-center mt-1 space-x-2 text-xs text-gray-500 dark:text-gray-400">
                                        <span>@post.PublishedAt.ToShortDateString()</span>
                                        <span>•</span>
                                        <span>@post.ReadTime min read</span>
                                    </div>
                                </div>
                                <button @onclick="() => NavigateToEditPost(post.Slug)" class="p-2 text-gray-400 hover:text-blue-600 dark:hover:text-blue-400">
                                    <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M11 5H6a2 2 0 00-2 2v11a2 2 0 002 2h11a2 2 0 002-2v-5m-1.414-9.414a2 2 0 112.828 2.828L11.828 15H9v-2.828l8.586-8.586z"></path>
                                    </svg>
                                </button>
                            </div>
                        </Animate>
                    }
                }
            </div>
        </div>

        <!-- Recent Activity -->
        <div class="bg-white dark:bg-gray-800 rounded-2xl p-6 shadow-sm hover:shadow-md transition-all duration-300">
            <div class="flex items-center justify-between mb-4">
                <h2 class="text-lg font-semibold text-gray-900 dark:text-white">Recent Activity</h2>
            </div>
            <div class="space-y-4">
                @if (RecentActivity == null || !RecentActivity.Any())
                {
                    <div class="text-center py-8">
                        <p class="text-gray-500 dark:text-gray-400">No recent activity</p>
                    </div>
                }
                else
                {
                    @foreach (var activity in RecentActivity)
                    {
                        <Animate Animation="Animations.FadeIn" Duration="TimeSpan.FromSeconds(0.3)">
                            <div class="flex items-center space-x-4 p-4 rounded-xl hover:bg-gray-50 dark:hover:bg-gray-700/50 transition-colors">
                                <div class="flex-1">
                                    <p class="text-sm text-gray-900 dark:text-white">
                                        @activity.Message
                                    </p>
                                    <div class="flex items-center mt-1 space-x-2 text-xs text-gray-500 dark:text-gray-400">
                                        <span>@activity.Timestamp.ToLocalTime().ToString("g")</span>
                                    </div>
                                </div>
                            </div>
                        </Animate>
                    }
                }
            </div>
        </div>
    </div>
</div>
